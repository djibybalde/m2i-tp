public class Expression {
    public static void main(String[] args) {
        int number1=12; // a variable
        int number2=24; // a variable

        int sum = number1+number2; // an expression
        // sum, number1 and number2 must have the type (int=int+int)

        double rayon = 12.5;
        double area = Math.pow(rayon, 2)*Math.PI;
    }
}
